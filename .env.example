# OpenAI-compatible Stage 03 planner config
# Copy this file to `.env` in the same directory and fill real values locally.

OPENAI_API_KEY=
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=
OPENAI_TIMEOUT_SECONDS=120

# Optional project-specific overrides can be added here later.

